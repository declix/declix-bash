import "package://pkl.declix.org/pkl-declix@0.3.0#/apt/apt.pkl"
import "package://pkl.declix.org/pkl-declix@0.3.0#/fs/fs.pkl"

resources = new Listing {
    // Install a basic container runtime
    new apt.Package {
        name = "curl"
        state = "installed"
    }
    
    // Create a simple test file
    new fs.File {
        path = "/tmp/test-container-config.txt"
        content = "container test file"
        mode = "0644"
        user = "root"
        group = "root"
    }
}